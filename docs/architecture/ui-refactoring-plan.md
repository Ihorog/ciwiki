# 🎨 UI Refactoring Plan — Cimeika Unified Interface

**Дата створення:** 2026-01-24 18:28:05  
**Автор:** Ci + @Ihorog  
**Статус:** 📋 До виконання

---

## 📊 Поточний стан

### Структура UI (станом на 2026-01-24)

```
frontend/src/
├── App.jsx — 11 маршрутів
├── layouts/MainLayout.jsx — глобальна навігація
├── pages/
│   ├── WelcomePage.jsx ❌ НЕ підключена
│   ├── Chat.jsx ✅
│   └── SeoDemo.tsx ⚠️
├── components/
│   ├── CiFAB/ ✅ АКТИВНИЙ
│   ├── CiButton/ ❌ ЗАСТАРІЛИЙ
│   ├── CiOverlay.jsx ❌ ДУБЛІКАТ
│   ├── CiEntryScreen.css ❌ СИРОTA
│   ├── ModuleStatusDashboard.tsx ⚠️ НЕ ВИКОРИСТОВУЄТЬСЯ
│   └── BackgroundTheme/ ⚠️ НЕДОРОБКА
└── modules/ (8 view компонентів) ✅
```

---

## ❌ Виявлені проблеми

### 1. Дублювання компонентів
- **CiButton** vs **CiFAB** — 2 floating кнопки
- **CiOverlay** дублює функціонал CiFAB
- **LegendCiView** vs **CiLegendsView** — 2 сторінки легенд

### 2. Незавершені компоненти
- `BackgroundTheme/` — динамічні фони реалізовані, але не інтегровані
- `ModuleStatusDashboard` — корисний, але не підключений
- `WelcomePage` — існує, але не в роутах

### 3. Конфлікт навігації
- Хедер MainLayout дублює CiFAB
- 2 сис��еми навігації роблять однакову роботу

### 4. Застарілі файли
- `CiEntryScreen.css` без компонента
- Gesture handler у CiButton не використовується

---

## 🎯 Цільова архітектура

### Принципи (згідно з Cimeika Canon)

#### 1. **ЄДИНИЙ ІНТЕРФЕЙС НАВІГАЦІЇ**
✅ **CiFAB** — єдина точка навігації (Floating Action Button)  
✅ **MainLayout** — спрощено до логотипу + breadcrumbs  
❌ Видалити дублікати (CiButton, CiOverlay)

#### 2. **МОДУЛЬНА СТРУКТУРА**
Кожен модуль = самодостатня одиниця:
```
modules/[ModuleName]/
├── views/[ModuleName]View.tsx — основний view
├── components/ — UI компоненти модуля
├── hooks/ — React hooks
├── api.ts — API клієнт
├── types.ts — TypeScript типи
└── ui.tsx — переприсутні UI елементи
```

#### 3. **УНІФІКОВАНА СТРУКТУРА VIEW**
Кожен модуль використовує:
```tsx
<div className="module-view [module-name]-view">
  <div className="module-view-header">
    <h1>{Title}</h1>
    <p className="subtitle">{Description}</p>
    <span className="module-view-status">{Status}</span>
  </div>
  <div className="module-view-content">
    {/* Контент модуля */}
  </div>
</div>
```

#### 4. **DOCUMENTATION FIRST**
- Документувати перед рефакторингом
- Кожен компонент має приклад використання
- Створити UI Kit у ciwiki

---

## 🔧 План рефакторингу

### PHASE 1: Очищення (Cleanup)

#### 1.1 Видалити застарілі компоненти
```bash
# Компоненти для видалення
frontend/src/components/CiButton/
frontend/src/components/CiOverlay.jsx
frontend/src/components/CiOverlay.css
frontend/src/components/CiEntryScreen.css
```

**Чому:**
- CiButton замінено на CiFAB
- CiOverlay дублює CiFAB
- CiEntryScreen.css — сирота без компонента

---

#### 1.2 Об'єднати дублікати
```
❌ LegendCiView (/ci/legend)
❌ CiLegendsView (/kazkar/legends)
↓
✅ LegendCiView — єдина сторінка легенд
Маршрут: /legends
```

---

#### 1.3 Інтегрувати WelcomePage
```tsx
// App.jsx
<Route path="/" element={<WelcomePage />} />
<Route path="/app" element={<MainLayout />}>  
  <Route index element={<Navigate to="/ci" replace />} />
  {/* решта роутів */}
</Route>
```

---

### PHASE 2: Систематизація (Unification)

#### 2.1 Спростити MainLayout
```tsx
// Поточний стан
<header>
  <Logo />
  <nav>7 модулів</nav> ❌ дублює CiFAB
</header>

// Цільовий стан
<header>
  <Logo />
  <Breadcrumbs /> ✅
</header>
```

---

#### 2.2 Завершити BackgroundTheme
- Інтегрувати в ThemeProvider
- Додати анімації переходів між модулями
- Документувати використання

---

#### 2.3 Підключити ModuleStatusDashboard
```tsx
// Додати маршрут для адмін-панелі
<Route path="/admin/status" element={<ModuleStatusDashboard />} />
```

---

#### 2.4 Уніфікувати стилі модулів
Всі модулі використовують:
```css
/* styles/moduleView.css */
.module-view { /* базові стилі */ }
.module-view-header { /* хедер */ }
.module-view-content { /* контент */ }
.module-view-status { /* статус badge */ }
```

---

### PHASE 3: Документування (Documentation)

#### 3.1 Створити UI Kit у ciwiki
```markdown
docs/ui-kit/
├── components.md — всі компоненти
├── navigation.md — система навігації
├── modules.md — структура модулів
└── theming.md — система тем
```

#### 3.2 Додати Storybook приклади
```bash
# Для кожного компонента
components/CiFAB/CiFAB.stories.tsx
components/ModuleView/ModuleView.stories.tsx
```

---

### PHASE 4: Тестування (Testing)

#### 4.1 Перевірити всі маршрути
```
✅ / — WelcomePage
✅ /ci — CiView
✅ /podija — PodijaView
✅ /nastrij — NastrijView
✅ /malya — MalyaView
✅ /kazkar — KazkarView
✅ /calendar — CalendarView
✅ /gallery — GalleryView
✅ /legends — LegendCiView
✅ /chat — Chat
✅ /admin/status — ModuleStatusDashboard
```

#### 4.2 Перевірити навігацію
- CiFAB доступний на всіх сторінках
- Всі посилання працюють
- Breadcrumbs відображаються коректно

#### 4.3 Перевірити теми
- BackgroundTheme змінюється при переході між модулями
- Кольори відповідають модулям
- Анімації плавні

---

## 📋 Чеклист виконання

### Phase 1: Cleanup
- [ ] Видалити `CiButton/`
- [ ] Видалити `CiOverlay.jsx` і `.css`
- [ ] Видалити `CiEntryScreen.css`
- [ ] Об'єднати `LegendCiView` та `CiLegendsView`
- [ ] Підключити `WelcomePage` до роутів

### Phase 2: Unification
- [ ] Спростити `MainLayout` (видалити дублікат навігації)
- [ ] Завершити `BackgroundTheme` інтеграцію
- [ ] Додати маршрут для `ModuleStatusDashboard`
- [ ] Уніфікувати стилі всіх модулів

### Phase 3: Documentation
- [ ] Створити UI Kit в `ciwiki`
- [ ] Додати Storybook stories
- [ ] Документувати кожен компонент

### Phase 4: Testing
- [ ] Протестувати всі маршрути
- [ ] Перевірити навігацію
- [ ] Перевірити теми та анімації

---

## 🚀 Наступні кроки

1. ✅ **Документ створено** (цей файл)
2. 🔄 **Затвердити план** з @Ihorog
3. 📝 **Створити PR** з рефакторингом Phase 1
4. 🧪 **Тестування** Phase 1
5. 🔁 **Повторити** для Phase 2-4

---

## 📖 Референси

- 🎨 DrawingCanvas: `spark/Ihorog/family-calendar` (не знайдено, потрібне уточнення URL)
- 📂 Поточна структура: `Ihorog/cimeika-unified/frontend/src/`
- 📚 Архітектура: `docs/ARCHITECTURE.md`

---

**Згідно з Cimeika Canon:**
- ✅ DOCUMENTATION FIRST — документ створено перед кодом
- ✅ ANTI-REPEAT PRINCIPLE — план видалення дублікатів
- ✅ EXECUTION FLOW — покроковий plan с чеклистами

**Чекаємо на затвердження для створення PR.**