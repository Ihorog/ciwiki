# CIT Voice - Система Сенсорних Сповіщень

## Огляд

**CIT Voice** (Голос CIT) - автономна система сенсорних сповіщень для екосистеми Cimeika. Система моніторить зміни стану в `manifest.json` та API endpoints, класифікує події згідно з семантичною онтологією та надсилає структуровані повідомлення в Telegram.

## Архітектура

### Компоненти

1. **Voice Engine** (`core/voice_engine.py`)
   - Центральний обробник подій
   - Моніторинг `manifest.json` через файлову систему
   - Періодичне опитування `api/state-visual`
   - Класифікація подій згідно з `ontology.json`
   - Розсилка подій по зареєстрованим обробникам

2. **Telegram Notifier** (`integrations/telegram_bot.py`)
   - Інтеграція з Telegram Bot API через aiogram
   - Форматування повідомлень за рівнями (1, 11, 111)
   - Автоматичне завантаження візуальних активів з репозиторію `media`
   - Інтерактивні кнопки для режиму "Чарівна пропозиція"

3. **Ontology** (`core/ontology.json`)
   - Семантична класифікація подій
   - Визначення рівнів пріоритету
   - Шаблони повідомлень
   - Мапінг подій на візуальні активи

## Рівні Сповіщень

### 🟢 [1] Фон
**Призначення**: Фонові процеси та синтез знань  
**Стиль**: Мінімальний, інформаційний  
**Приклад**: "Організм проводить фоновий синтез знань..."

### 🟡 [11] Дія
**Призначення**: Активні пропозиції та оптимізації  
**Стиль**: Проактивний, з можливістю взаємодії  
**Візуальні активи**: ✅  
**Інтерактивність**: ✅  
**Приклад**: "Пропоную активувати модуль [Назва]. Це оптимізує [Ціль]..."

### 🔴 [111] Критично
**Призначення**: Критичні події та структурні прогалини  
**Стиль**: Максимальна увага, щільність 111  
**Візуальні активи**: ✅  
**Приклад**: "УВАГА: Виявлено структурну прогалину. Сформовано нове ТЗ на GitHub."

## Встановлення

### 1. Залежності

```bash
# Python 3.9+
pip install -r requirements.txt
```

Додані залежності:
- `aiogram>=3.3.0` - Telegram Bot framework
- `httpx>=0.25.0` - Асинхронні HTTP запити
- `watchdog>=3.0.0` - Моніторинг файлової системи
- `python-dotenv>=1.0.0` - Управління конфігурацією

### 2. Конфігурація Telegram Bot

#### Створення бота

1. Знайдіть [@BotFather](https://t.me/BotFather) в Telegram
2. Виконайте команду `/newbot`
3. Дайте назву боту (наприклад: "CIT Voice")
4. Дайте username боту (має закінчуватись на `bot`, наприклад: `cit_voice_bot`)
5. Збережіть отриманий **Bot Token**

#### Отримання Chat ID

Варіант 1 - Особистий чат:
1. Напишіть боту будь-яке повідомлення
2. Відкрийте: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
3. Знайдіть `"chat":{"id":...}` - це ваш Chat ID

Варіант 2 - Канал/група:
1. Додайте бота до каналу/групи як адміністратора
2. Напишіть повідомлення в канал/групу
3. Відкрийте: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
4. Chat ID для каналів зазвичай негативний (наприклад: `-1001234567890`)

### 3. Налаштування конфігурації

```bash
# Копіювати шаблон
cp .env.example .env

# Відредагувати .env
nano .env
```

Заповніть обов'язкові поля в `.env`:
```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
```

## Використання

### Запуск системи

```bash
python cit_voice.py
```

### Тестування компонентів окремо

#### Voice Engine
```bash
python core/voice_engine.py core/ontology.json public/manifest.json
```

#### Telegram Bot
```bash
python integrations/telegram_bot.py
```

### Команди бота в Telegram

- `/start` - Активація бота
- `/status` - Перевірка статусу системи

### Генерація тестової події

Приклад Python коду для генерації події:

```python
import asyncio
from core.voice_engine import VoiceEngine
from integrations.telegram_bot import TelegramNotifier

async def test_event():
    engine = VoiceEngine("core/ontology.json", "public/manifest.json")
    notifier = TelegramNotifier(bot_token="YOUR_TOKEN", chat_id="YOUR_CHAT_ID")
    engine.register_handler(notifier)
    
    # Тестова подія рівня 11 (Дія)
    await engine.emit_intent_event({
        'description': 'Виявлено необхідність оптимізації процесу',
        'action': 'Активувати модуль автоматизації',
        'module': 'AutoOptimizer',
        'goal': 'продуктивність на 30%'
    })

asyncio.run(test_event())
```

## Режим "Чарівна Пропозиція"

Коли `IntentObserver` фіксує потребу, система генерує інтерактивне повідомлення з кнопками:

```
🟡 [11] (Дія): Пропоную активувати модуль AutoOptimizer. 
Це оптимізує продуктивність на 30%...

[✅ ПРИЙНЯТИ ВОЛЮ] [❌ ВІДХИЛИТИ]
```

При натисканні кнопки:
- **ПРИЙНЯТИ ВОЛЮ**: Система отримує підтвердження та може ініціювати дію
- **ВІДХИЛИТИ**: Подія відхиляється без дії

## Інтеграція з Media Repository

Для рівнів 11 та 111 система автоматично завантажує візуальні активи з репозиторію [Ihorog/media](https://github.com/Ihorog/media).

### Мапінг подій на медіа:
- `module_proposal` → `icons/action.png`
- `structural_gap` → `icons/critical.png`
- `intent_detected` → `icons/intent.png`

Медіа кешуються локально в `/tmp/cit_media_cache` для швидкого доступу.

## Розширення Системи

### Додавання нового типу події

1. Відредагуйте `core/ontology.json`:
```json
{
  "event_types": {
    "your_event": {
      "level": "11",
      "template": "Ваш шаблон повідомлення з {параметром}",
      "keywords": ["key", "words"],
      "requires_media": true,
      "interactive": false
    }
  }
}
```

2. Додайте медіа файл (опціонально):
```python
# В integrations/telegram_bot.py
media_map = {
    'your_event': 'icons/your_icon.png',
    # ...
}
```

### Додавання нового обробника (не Telegram)

```python
class YourHandler:
    async def handle_event(self, event: Dict[str, Any]):
        # Ваша логіка обробки
        pass

# Реєстрація
engine.register_handler(YourHandler())
```

## Стиль Повідомлень

Згідно з ТЗ, система дотримується принципу **максимальної щільності 111**:

- ❌ Ввічливість, привітання, надлишкові слова
- ✅ Факти, конкретика, можливості
- ✅ Стислість, чіткість, актуальність

**Приклад правильного стилю:**
> 🔴 [111] (Критично): УВАГА: Структурна прогалина. ТЗ сформовано. GitHub #42.

**Приклад неправильного стилю:**
> ❌ "Добрий день! Хотів би повідомити, що, можливо, виявлена якась проблема..."

## Безпека

- ⚠️ **НЕ КОМІТЬТЕ** `.env` файл до Git
- ⚠️ Додайте `.env` до `.gitignore`
- ✅ Використовуйте `.env.example` як шаблон
- ✅ Зберігайте Bot Token в безпечному місці
- ✅ Обмежте доступ до чату/каналу сповіщень

## Моніторинг та Логування

Система логує всі події в stdout:

```
2026-01-25 17:30:00 - voice_engine - INFO - Processing event: level=11, type=module_proposal
2026-01-25 17:30:01 - telegram_bot - INFO - Message sent successfully: level=11, type=module_proposal
```

Рівень логування налаштовується через `LOG_LEVEL` в `.env`:
- `DEBUG` - Детальна інформація для розробки
- `INFO` - Стандартний рівень (рекомендовано)
- `WARNING` - Тільки попередження
- `ERROR` - Тільки помилки

## Troubleshooting

### Проблема: Bot не надсилає повідомлення

**Рішення:**
1. Перевірте Bot Token в `.env`
2. Перевірте Chat ID (для каналів зазвичай негативний)
3. Переконайтесь що бот доданий до каналу як адміністратор
4. Перевірте логи на помилки

### Проблема: Медіа не завантажуються

**Рішення:**
1. Перевірте доступ до репозиторію `media`
2. Переконайтесь що файли існують за вказаними шляхами
3. Перевірте наявність прав запису в `/tmp/cit_media_cache`

### Проблема: Manifest.json не моніториться

**Рішення:**
1. Переконайтесь що файл існує
2. Перевірте права доступу до файлу
3. На деяких ОС може знадобитись підвищення прав для watchdog

## API для Інтеграції

### Ручна генерація події

```python
await engine.emit_intent_event({
    'description': 'Опис наміру',
    'action': 'Запропонована дія',
    'module': 'Назва модуля',
    'goal': 'Мета оптимізації'
})
```

### Генерація події з коду

```python
await engine.process_event({
    'type': 'knowledge_synthesis',
    'source': 'custom',
    'description': 'Ваш опис події'
})
```

## Майбутні Покращення

- [ ] Підтримка множинних каналів/чатів
- [ ] Веб-панель моніторингу подій
- [ ] Інтеграція з іншими месенджерами (Discord, Slack)
- [ ] Збереження історії подій в БД
- [ ] Аналітика та статистика сповіщень
- [ ] Гнучка система фільтрів подій

## Ліцензія

Частина екосистеми Cimeika. Дотримуйтесь правил `COPILOT_CANON.md`.

## Підтримка

Для питань та issues:
- Створіть issue в [ciwiki](https://github.com/Ihorog/ciwiki/issues)
- Додайте label `voice-module`
- Опишіть проблему детально з логами

---

**Audit**: Created 2026-01-25 — CIT Voice Module Initial Implementation
